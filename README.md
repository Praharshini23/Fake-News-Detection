# Fake-News-Detection
The system uses a labeled dataset of news articles. After preprocessing (tokenization, stopword removal), it converts text into numeric features using TF-IDF vectorization. Then, it trains a Logistic Regression classifier to predict whether a news piece is fake or real. It provides a simple command-line or web UI for input and results.
